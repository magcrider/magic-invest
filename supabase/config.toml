# Supabase Edge Functions Configuration

[functions.fetch-banrep-data]
verify_jwt = false  # cron job, no requiere autenticación

[functions.backfill-historical-data]
verify_jwt = false  # invocación manual, no requiere autenticación
